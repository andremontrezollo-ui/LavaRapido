/**
 * Mixing UI barrel — re-exports the component used as the main page entry.
 * Actual React components live in src/components/mixing/ (shared component library).
 */

export { default as MixingPage } from "@/pages/MixingPage";
