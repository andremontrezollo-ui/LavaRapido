/**
 * Address Generator Module
 * 
 * Responsible for generating deposit addresses, issuing one-time tokens,
 * and managing address lifecycle (expiration, validation).
 * 
 * Emits: ADDRESS_TOKEN_EMITTED, ADDRESS_TOKEN_RESOLVED
 */

export * from './domain';
export * from './application';
export * from './infra';
